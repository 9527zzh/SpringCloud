server:
  port: 3344

spring:
  application:
    name:  microservicecloud-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ZeroVV/microservicecloud-config.git #GitHub上面的git仓库名字

#“---”分隔符下面一定要有一段空格
